---
phase: 06-page-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pages/Login.tsx
  - pages/Dashboard.tsx
autonomous: true

must_haves:
  truths:
    - "Login page uses token typography (text-heading for logo, text-body for labels)"
    - "Dashboard page uses PageHeader component with size='default'"
    - "Dashboard spacing uses token values instead of raw Tailwind"
    - "No arbitrary Tailwind text sizes remain in Login.tsx or Dashboard.tsx"
  artifacts:
    - path: "pages/Login.tsx"
      provides: "Token-aligned login page"
      contains: "text-heading"
    - path: "pages/Dashboard.tsx"
      provides: "Token-aligned dashboard with PageHeader"
      contains: "PageHeader"
  key_links:
    - from: "pages/Dashboard.tsx"
      to: "components/patterns/PageHeader.tsx"
      via: "import and usage"
      pattern: "import.*PageHeader"
---

<objective>
Migrate Login and Dashboard pages to canonical design system patterns.

Purpose: These are the two cleanest pages (zero arbitrary values). Migrating them first establishes the pattern for subsequent plans and validates that PageHeader works correctly in real usage.

Output: Login.tsx and Dashboard.tsx fully aligned with token system and composition patterns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-page-migration/06-RESEARCH.md
@components/patterns/PageHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Login.tsx to token typography</name>
  <files>pages/Login.tsx</files>
  <action>
    Replace hardcoded typography classes with design tokens:

    1. Logo heading: `text-2xl font-bold tracking-tight` -> `text-heading font-bold tracking-tight`
       (text-heading = 18px, appropriate for logo mark -- not text-title because Login is not a "destination page" with a full header)

    2. Card heading: `text-xl font-semibold` -> `text-heading font-semibold`
       (Same token for modal/card headings)

    3. Keep ALL other classes as-is. Login already uses semantic colors (text-foreground, text-muted-foreground, bg-background, bg-card etc.). No arbitrary values exist.

    Do NOT add PageHeader to Login -- it has a centered card layout, not a page header pattern.
    Do NOT change the form layout, button styling, or spacing -- they are already clean.
  </action>
  <verify>
    Run `npx tsc --noEmit 2>&1 | grep -i "Login"` -- no TypeScript errors.
    Grep pages/Login.tsx for `text-2xl` and `text-xl` -- should find zero occurrences.
    Grep pages/Login.tsx for `text-heading` -- should find 2 occurrences.
  </verify>
  <done>Login.tsx uses only token typography classes. No raw size classes (text-2xl, text-xl) remain.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Dashboard.tsx to PageHeader and token spacing</name>
  <files>pages/Dashboard.tsx</files>
  <action>
    1. Add import: `import { PageHeader } from '../components/patterns/PageHeader'`

    2. Replace the manual header block:
       ```
       <div>
         <h1 className="text-3xl font-bold text-foreground tracking-tight">{t('title')}</h1>
         <p className="text-muted-foreground mt-2 font-light">{t('subtitle')}</p>
       </div>
       ```
       With:
       ```
       <PageHeader title={t('title')} description={t('subtitle')} size="default" />
       ```

    3. In StatCard component, replace `text-xs` on CardTitle with `text-overline` (the uppercase tracking-wider label style is exactly what text-overline is for).

    4. Replace `text-3xl` on stat value with `text-title` (30px, matches "destination page" large number display).

    5. Section heading "Recent Books": `text-lg font-bold` -> `text-heading font-bold` (18px section heading).

    6. Keep `space-y-12` as-is on the outer div (48px = generous spacing, matches the "Generous" design language).

    Do NOT change the stats grid layout, card hover effects, or chart section.
    Do NOT change the recent books card layout or image styling.
  </action>
  <verify>
    Run `npx tsc --noEmit 2>&1 | grep -i "Dashboard"` -- no TypeScript errors.
    Grep pages/Dashboard.tsx for `text-3xl` -- should find zero occurrences.
    Grep pages/Dashboard.tsx for `PageHeader` -- should find 1 import + 1 usage.
    Grep pages/Dashboard.tsx for `text-title` -- should find 1 occurrence (stat value).
  </verify>
  <done>Dashboard.tsx uses PageHeader with size='default', token typography throughout. No raw heading sizes (text-3xl, text-lg) remain.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` shows no new errors in Login.tsx or Dashboard.tsx
- No `text-2xl`, `text-xl`, `text-3xl`, or `text-lg` remain in these two files
- PageHeader is imported and used correctly in Dashboard
- Both pages render without visual breakage (structure unchanged, only class names updated)
</verification>

<success_criteria>
- Login.tsx: 2 typography replacements, zero arbitrary values
- Dashboard.tsx: PageHeader integrated, 3-4 typography replacements, zero arbitrary values
- Both files compile with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-page-migration/06-01-SUMMARY.md`
</output>
