---
phase: 06-page-migration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - pages/StudySession.tsx
autonomous: true

must_haves:
  truths:
    - "All text-[9px] in StudySession.tsx replaced with text-overline"
    - "All text-[10px] in StudySession.tsx replaced with text-overline"
    - "Serif font (font-serif) preserved on highlight text and notes"
    - "Italic styling preserved on blockquote"
    - "Rating button colors (red, amber, blue, green) preserved as intentional semantic choices"
    - "Full-screen fixed layout preserved"
  artifacts:
    - path: "pages/StudySession.tsx"
      provides: "Token-aligned study session preserving intentional deviations"
  key_links: []
---

<objective>
Migrate StudySession page to token typography while preserving intentional design deviations.

Purpose: StudySession is the immersive study experience with intentional deviations from the design system (serif font, rating colors, full-screen layout). This plan carefully replaces only arbitrary values while explicitly preserving these deviations. The file is 713 lines, warranting its own plan.

Output: StudySession.tsx with zero arbitrary text values but all intentional deviations preserved.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-page-migration/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace arbitrary text sizes in StudySession.tsx</name>
  <files>pages/StudySession.tsx</files>
  <action>
    **PRESERVE (do NOT change these -- intentional deviations):**
    - `font-serif italic text-lg md:text-xl` on blockquote (line 540) -- literary reading experience
    - `font-serif` on edit textarea (line 531) -- consistency with display
    - `font-serif text-lg` on note display (line 586) -- same reading experience
    - `font-serif italic text-lg` on divider tilde (line 580) -- decorative element
    - Rating button colors: `bg-red-500`, `bg-amber-500`, `bg-blue-500`, `bg-green-500` -- semantic user feedback
    - `h-screen flex flex-col` full-screen layout -- core UX pattern
    - `text-lg md:text-xl` for highlight text -- intentionally larger for reading

    **REPLACE text-[9px] -> text-overline (all instances):**
    These are tag badges in the desktop and mobile tag display sections:
    - Line 454: `text-[9px]` in desktop tag span -> `text-overline`
    - Line 465: `text-[9px]` in desktop "more tags" button -> `text-overline`
    - Line 497: `text-[9px]` in mobile tag span -> `text-overline`
    - Line 508: `text-[9px]` in mobile "more tags" button -> `text-overline`

    **REPLACE text-[10px] -> text-overline (all instances):**
    These are labels and UI chrome:
    - Line 522: Edit highlight label `text-[10px]` -> `text-overline`
    - Line 535: Save hint `text-[10px]` -> `text-overline`
    - Line 560: Edit note label `text-[10px]` -> `text-overline`
    - Line 574: Save hint `text-[10px]` -> `text-overline`
    - Line 607: Note edit button label `text-[10px]` -> `text-overline`
    - Line 639: "Again" keyboard hint `text-[10px]` -> `text-overline`
    - Line 648: "Hard" keyboard hint `text-[10px]` -> `text-overline`
    - Line 655: "Good" keyboard hint `text-[10px]` -> `text-overline`
    - Line 663: "Easy" keyboard hint `text-[10px]` -> `text-overline`

    Total: 4 text-[9px] + 9 text-[10px] = 13 replacements.

    Do NOT add PageHeader -- StudySession has a custom compact header with back button, progress bar, and action icons. This is an intentional deviation (immersive study mode).
    Do NOT change any event handlers, keyboard shortcuts, or state management.
    Do NOT change the progress bar, tag selector modal, or delete popover integration.
  </action>
  <verify>
    Grep pages/StudySession.tsx for `text-\[9px\]` -- should find zero occurrences.
    Grep pages/StudySession.tsx for `text-\[10px\]` -- should find zero occurrences.
    Grep pages/StudySession.tsx for `text-overline` -- should find 13 occurrences.
    Grep pages/StudySession.tsx for `font-serif` -- should still find 3-4 occurrences (preserved).
    Grep pages/StudySession.tsx for `bg-red-500` -- should still find 1 occurrence (preserved rating color).
    Run `npx tsc --noEmit 2>&1 | grep -i "StudySession"` -- no TypeScript errors.
  </verify>
  <done>StudySession.tsx has zero arbitrary text-[Npx] values. All 13 instances replaced with text-overline. Serif font, rating colors, and full-screen layout preserved intact.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` shows no new errors in StudySession.tsx
- Zero `text-[9px]` or `text-[10px]` remain
- All intentional deviations preserved: font-serif (3-4 occurrences), rating colors (4 button colors), h-screen layout
</verification>

<success_criteria>
- StudySession.tsx: 13 arbitrary values replaced, zero remain
- Intentional deviations explicitly preserved and verified
- File compiles with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-page-migration/06-04-SUMMARY.md`
</output>
