---
phase: 03-critical-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [components/DeckTable.tsx]
autonomous: false

must_haves:
  truths:
    - "DeckTable uses semantic tokens for structural elements"
    - "Table header uses bg-muted and text-muted-foreground"
    - "Row hover uses hover:bg-accent/50 pattern"
    - "SRS stat colors (blue/amber/green) are preserved"
  artifacts:
    - path: "components/DeckTable.tsx"
      provides: "Deck table with semantic tokens"
      contains: "border-border"
  key_links:
    - from: "components/DeckTable.tsx"
      to: "tailwind semantic tokens"
      via: "CSS class names"
      pattern: "bg-muted|border-border|text-foreground"
---

<objective>
Migrate DeckTable.tsx to semantic color tokens.

Purpose: DeckTable is a child component of Study.tsx - migrating it completes the table styling portion of the Study page.
Output: DeckTable.tsx with semantic tokens for structure while preserving SRS-meaningful stat colors.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-critical-pages/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate DeckTable structural elements to semantic tokens</name>
  <files>components/DeckTable.tsx</files>
  <action>
Replace hardcoded zinc colors with semantic tokens:

1. Container border (line 20):
   - BEFORE: `border-zinc-200 dark:border-zinc-800`
   - AFTER: `border-border`

2. Header background (line 22):
   - BEFORE: `bg-zinc-50 dark:bg-zinc-950`
   - AFTER: `bg-muted`

3. Header border (line 22):
   - BEFORE: `border-zinc-200 dark:border-zinc-800`
   - AFTER: `border-border`

4. Header text (lines 23-27, 5 instances):
   - BEFORE: `text-zinc-600 dark:text-zinc-400`
   - AFTER: `text-muted-foreground`

5. Body divider (line 31):
   - BEFORE: `divide-zinc-100 dark:divide-y dark:divide-zinc-800`
   - AFTER: `divide-border`

6. Row hover (line 38):
   - BEFORE: `hover:bg-zinc-50 dark:hover:bg-zinc-800`
   - AFTER: `hover:bg-accent/50`

7. All Books row background (line 39):
   - BEFORE: `bg-zinc-50/50 dark:bg-zinc-800/50`
   - AFTER: `bg-muted/50`

8. Title text (line 45):
   - BEFORE: `text-zinc-900 dark:text-zinc-100`
   - AFTER: `text-foreground`

9. Author text (line 51):
   - BEFORE: `text-zinc-400 dark:text-zinc-500`
   - AFTER: `text-muted-foreground`

10. Zero stats and total text (lines 60, 68, 76, 84 - where count is 0 or for total display):
    - BEFORE: `text-zinc-400 dark:text-zinc-500`
    - AFTER: `text-muted-foreground`

IMPORTANT: KEEP the colored stat numbers (text-blue-600, text-amber-600, text-green-600) - these are SRS-meaningful colors for New/Learning/Review counts.
  </action>
  <verify>Run `npm run build` to confirm no TypeScript errors</verify>
  <done>DeckTable uses semantic tokens for structure while preserving SRS stat colors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>DeckTable.tsx migrated to semantic tokens with preserved SRS colors</what-built>
  <how-to-verify>
1. Run `npm run dev`
2. Navigate to http://localhost:3000/#/study
3. Scroll down to see the book list table
4. Verify in LIGHT mode:
   - Table header has subtle background (muted)
   - Table border is visible but not harsh
   - Row hover shows subtle highlight
   - Book titles are clearly readable
   - Author text is dimmer (muted-foreground)
5. Toggle to DARK mode and verify same elements
6. Verify stat colors:
   - New count is blue
   - Learning count is amber/orange
   - Review count is green
   - Zero values are muted
  </how-to-verify>
  <resume-signal>Type "approved" to continue to 03-03-PLAN.md, or describe issues for revision</resume-signal>
</task>

</tasks>

<verification>
## Automated Checks

```bash
npm run build
grep -n "zinc-200\|zinc-800\|zinc-50\|zinc-950" components/DeckTable.tsx
# Should return empty (no hardcoded structural colors)
```

## Visual Verification

- [ ] Table header background visible in both themes
- [ ] Row hover effect works in both themes
- [ ] Title text readable in both themes
- [ ] Author text appropriately dimmed
- [ ] Blue/amber/green stat colors preserved
</verification>

<success_criteria>
1. DeckTable uses border-border, bg-muted, text-foreground, text-muted-foreground
2. Row hover uses hover:bg-accent/50
3. SRS stat colors (blue/amber/green) are preserved
4. User has approved the visual appearance in both themes
</success_criteria>

<output>
After completion, create `.planning/phases/03-critical-pages/03-02-SUMMARY.md`
</output>
